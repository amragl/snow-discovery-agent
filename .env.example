# --------------------------------------------------------------------------
# ServiceNow Instance Configuration
# --------------------------------------------------------------------------
# The base URL of your ServiceNow instance (must be HTTPS).
SERVICENOW_INSTANCE=https://your-instance.service-now.com

# --------------------------------------------------------------------------
# Authentication (Basic Auth)
# --------------------------------------------------------------------------
# A ServiceNow user with the discovery_admin role (or equivalent).
SERVICENOW_USERNAME=your-username
SERVICENOW_PASSWORD=your-password

# --------------------------------------------------------------------------
# Optional Settings
# --------------------------------------------------------------------------
# Request timeout in seconds (default: 30)
# SERVICENOW_TIMEOUT=30

# Maximum results per API query (default: 100)
# SERVICENOW_MAX_RESULTS=100

# Logging level: DEBUG, INFO, WARNING, ERROR (default: INFO)
# SERVICENOW_LOG_LEVEL=INFO

# --------------------------------------------------------------------------
# Startup Validation
# --------------------------------------------------------------------------
# When true the server validates ServiceNow connectivity on startup.
# Set to false for offline development.
# VALIDATE_ON_STARTUP=true
